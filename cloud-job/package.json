{
  "name": "betvalue-cloud-job",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "GitHub Actions job that prepares BetValue Analyzer cloud results for Firestore.",
  "scripts": {
    "start": "node index.mjs",
    "check": "node --check index.mjs && node --check multi-ai.mjs && node --check deploy-rules.mjs && node --check test-multi-ai.mjs && npm run test:multi-ai",
    "test:multi-ai": "node test-multi-ai.mjs"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0"
  },
  "engines": {
    "node": ">=20"
  }
}
